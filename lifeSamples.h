#pragma once

#include "lifeGrid.h"
#include <unordered_map>

namespace LifeSamples {
	static std::unordered_map<std::string, std::string> samples({
		{ "Glider", "(1, 0)\n(2, 1)\n(2, 2)\n(1, 2)\n(0, 2)" }
	});
}